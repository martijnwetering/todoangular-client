import { AddOnEnterDirective } from './add-on-enter.directive';

describe('AddOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new AddOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
